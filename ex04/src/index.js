const milili = [10, 25, 4]; // Don not

function myArr(mili) {
    'use strict';
    // Only
    // Using mili = [4, 10, 25] invalid

    // Only
    return mili;
}
console.log(myArr()); // Only
module.exports = myArr;